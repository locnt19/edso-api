export const PaginateDefs = `
    totalDocs: Int,
    offset: Int,
    limit: Int,
    totalPages: Int,
    page: Int,
    pagingCounter: Int,
    hasPrevPage: Boolean,
    hasNextPage: Boolean,
    prevPage: Int,
    nextPage: Int
`;
